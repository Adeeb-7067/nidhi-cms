# Force same-origin API in local Vite (overrides frontend/.env).
# Requests go to /api and are proxied to the backend — no CORS OPTIONS per call.
# Delete this file or comment the line below when intentionally using a split API origin.
VITE_API_BASE_URL=
